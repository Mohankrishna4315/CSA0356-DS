#include <stdio.h>
int main()
{
int num;
printf("Enter the number:");
scanf("%d",&num);
if(num>0||num<0)
{
if(num%2==0)
{
printf("Even ");
}
else
{
printf("Odd ");
}
}
else{
printf("Charcters are not allowed:");
printf("Enter the valid input:");
}
return 0;
}
